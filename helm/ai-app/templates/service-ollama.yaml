apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-app.fullname" . }}-ollama
  labels:
    {{- include "ai-app.labels" . | nindent 4 }}
    component: ollama
spec:
  type: {{ .Values.ollama.service.type }}
  ports:
    - port: {{ .Values.ollama.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ai-app.selectorLabels" . | nindent 4 }}
    component: ollama
